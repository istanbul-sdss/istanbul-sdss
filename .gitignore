# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
# Coverage artefaktları (pytest-cov)
.coverage
.coverage.*
coverage.xml
htmlcov/

# ── Sanal ortam ───────────────────────────────────────────────────────────
venv/
.venv/
env/
ENV/

# ── IDE / editör ──────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Claude Code ayarları ─────────────────────────────────────────────────
# Tüm editör/oturum state'i repo dışı: lokal ayarlar + geçici worktree'ler.
.claude/

# ── Streamlit ─────────────────────────────────────────────────────────────
# Yerel/deploy secrets (token, API key vb.) ASLA repo'ya girmemeli.
# NOT: proje şu an hiç secret okumuyor (Overpass anonim, TÜİK CSV
# yüklenirken kullanıcı seçimi); bu kural ileride API entegrasyonu
# eklendiğinde devreye girecek bir korkuluktur.
.streamlit/secrets.toml

# ── Uygulama çıktıları ────────────────────────────────────────────────────
# OSMnx / Overpass yanıt cache'i (yeniden indirilebilir, büyük)
cache/
# Yürüme graph cache'i (GraphML, ilçe başına 50-300 MB)
cache/graphs/
data/graphs/
# Streamlit'ten üretilen Excel / analiz çıktıları (kullanıcı-spesifik)
output/
# Uygulama logları
logs/
# Build/release artefaktları (build_release.py tarafından üretilir)
dist/

# ── Yedek / geçici ────────────────────────────────────────────────────────
# Neighborhood Sync sayfasının ürettiği eski-dosya yedekleri
*.geojson.bak.*
*.bak

# ── OS özel ───────────────────────────────────────────────────────────────
*.log
*.tmp

# ── Tek seferlik veri-hazırlama / keşif scriptleri ve çıktıları ──────────
# Beşiktaş toplanma alanları için elle yazılmış, hardcoded-path'li deneme
# scriptleri ve ürettikleri ara/çıktı dosyaları. Projenin src/ mimarisinin
# parçası DEĞİL; üretim akışına girmezler. Diskte kalırlar, repoya girmezler.
fetch_buildings.py
fetch_overpass.py
match_osm.py
process_areas.py
build_final.py
overpass_buildings.json
overpass_polys.json
toplanma_alanlari.json
toplanma_alanlari.csv
toplanma_alanlari.xlsx
Toplanma_Alanlari_*.xlsx
_areas_pass1.pkl
# Name Lookup / extraction denemelerinden kalan ad-hoc Excel'ler (kök dizinde)
/Beşiktaş*.xlsx
# Dahili inceleme raporu (bulgular içerir — public repoda tutulmaz)
PROJE_INCELEME_RAPORU.md

# ── İSTİSNALAR (committedlanması gerekenler) ─────────────────────────────
# data/samples/ — hocaya gösterim için pre-bundled örnek çıktılar
# (ana .gitignore output/ ve cache/ engelliyor; data/ tracked)
!data/samples/
!data/samples/**
